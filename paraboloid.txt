z = (x^2 + y^2) / curve

y = (x^2 + z^2) / curve
________________________________________________________________________________________________________

hit = r.origin + t * r.dir

x^2 + z^2 - (y * curve) = 0
________________________________________________________________________________________________________

(r.origin.x + t * r.dir.x)^2 + (r.origin.z + t * r.dir.z)^2 - (r.origin.y + t * r.dir.y) * curve = 0
________________________________________________________________________________________________________

(r.origin.x^2 + (t * r.dir.x)^2 + 2 * (r.origin.x * (t * r.dir.x))) +
(r.origin.z^2 + (t * r.dir.z)^2 + 2 * (r.origin.z * (t * r.dir.z))) -
(r.origin.y + t * r.dir.y) * curve = 0
________________________________________________________________________________________________________

(r.dir.x^2 + r.dir.z^2) * t^2 + 
((2 * r.dir.x * r.origin.x) + (2 * r.dir.z * r.origin.z) - (r.dir.y * curve)) * t +
r.origin.x^2 + r.origin.z^2 - (r.origin.y * curve) = 0
________________________________________________________________________________________________________